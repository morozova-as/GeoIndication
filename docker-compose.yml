version: '3'

services:
  frontend:
    build: frontend/
    ports:
      - "5000:5000"
    command: npm run start-docker
  backend:
    build: backend/
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000


